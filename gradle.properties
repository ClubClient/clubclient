org.gradle.jvmargs=-Xmx1G
org.gradle.parallel=true
org.gradle.configuration-cache=false

# minecraft_version / yarn_mappings / fabric_api_version live in versions/<ver>/gradle.properties —
# they are the ONE thing that differs per node, and a copy here would be the copy that goes stale.
loader_version=0.19.3
loom_version=1.17-SNAPSHOT

mod_version=0.1.5
maven_group=com.club
archives_base_name=club