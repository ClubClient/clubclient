# Club — UI V2: Stage 11 — Icon System, Category Colors, Card Redesign, Logo

> **Status: APPROVED (2026-07-02).** План и палитра утверждены владельцем в диалоге (мокапы
> red-концепта = референс стиля, НЕ утверждённые цвета/контент). Пояснения — русский,
> идентификаторы/код — English. Контрольные точки владельца: после 11.2 (техника), 11.3 (стиль
> иконок + лого), 11.4 (карточка в игре).

## 0. Цель

Карточки модулей сейчас — только имя, читаются пусто. Стадия добавляет: **иконку** каждому модулю,
**ghost-подложку** (та же иконка, крупно, еле заметно), **цвет категории** как идентичность,
**полосу состояния** вместо любых надписей, заменяет процедурные иконки rail/поиска на новый набор
и даёт слову CLUB настоящий **логотип**.

## 1. Палитра категорий — «Вариант A» (УТВЕРЖДЕНА)

Токен `Tokens.categories()` (`theme/CategoryAccents.java`), значения в `ClubDark`:

| Категория | Hex | Ассоциация |
|---|---|---|
| Combat | `#C9808A` | приглушённый красный — атака/урон |
| Visuals | `#9E8BD9` | приглушённый фиолетовый — рендер; преемственность с violet-кромкой вкл-карточки |
| Player | `#7FBFA6` | холодная бирюза-зелень — тело/сам игрок |
| Misc | `#8C9BB5` | серо-стальной — инструменты, самая тихая |

Все выровнены по светлоте/приглушённости (калибровка — steel-blue `#86A6CC` HP-бара).

**Правила цвета (утверждены; расширены владельцем 2026-07-02 — 11.9):**
- **Категорийный цвет = идентичность И интерактив внутри категории:** чип/полоса/ghost/rail-индикатор
  ПЛЮС поповер настроек модуля (слайдеры, тумблеры, кнопки, сегмент-табы, выбранная опция дропдауна,
  фокус-кольца) и подсветка поиска (рамка/лупа/каретка при фокусе). Механика: `accent(int)`-override
  у Button/Toggle/Checkbox/Slider (0 = тема), `WidgetPaint.acc()/accHi()`.
- **Бренд `#7CABFF` остаётся:** логотип/шапка, HUD-редактор и HUD-элементы, любой контекст вне категории.
- **Текст никогда не красится** категорийным цветом — исключение (санкционировано v2.5 для «active
  segment»): активная подпись сегмент-таба и выбранная опция дропдауна.
- Off-канал (серые состояния) — из существующих токенов (textDesc/textFaint/border), решается в 11.4;
  новых серых hex не вводить без необходимости.

## 2. Карточка модуля — анатомия и состояния (УТВЕРЖДЕНО)

```
┌────────────────────────────────┐
│ ┌────┐                    ░░░░ │   чип 30×30 (radius 8, категорийный цвет @ ~12% фон,
│ │icon│  Module Name      ░ghost│   иконка ~17px категорийным цветом)
│ └────┘                    ░░░░ │   ghost: та же иконка 3–4×, alpha ~5–8%, у правого края,
│  ▬▬▬ ← полоса                  │   клипится границей карточки
└────────────────────────────────┘
```

- **Полоса состояния:** короткий штрих (~16×3), **геометрически по центру под чипом** (на мокапе
  была кривая — у нас точно по центру). Живёт ВСЕГДА; меняется только цвет: серый = выкл,
  категорийный = вкл; цвет едет существующим `Transition` (серый ↔ цвет) — ничего не прыгает.
- **Полоса — подтверждение, не единственный сигнал:** выкл = серая иконка + приглушённое имя +
  бледный ghost; вкл = цветная иконка + яркое имя + заметнее ghost.
- **Никакого текста «Enabled/Disabled»** на карточках.
- LMB = toggle, RMB = поповер — поведение Stage 6 не меняется.

## 3. Иконки — стиль и формат исходников

- Сетка **24×24**, штрих **2px**, круглые концы и стыки (`stroke-linecap/linejoin: round`),
  монохром (один цвет — красится в рантайме).
- Исходники: `tools/icons/src/<name>.svg`. Перед сдачей штрихи **переводятся в заливки**
  (outline stroke) и объединяются в **один составной path** — требование msdfgen.
- Диагонали разрешены и приветствуются: ограничение R8 (процедурные иконки без диагоналей)
  на MSDF-путь **не распространяется** — оно остаётся только историей.
- Набор (первая волна → полный):
  - **Модули (~8–10):** Animations, Screen Stretch, No Hurt Cam, No Fire Overlay, No Bobbing,
    Hands, HUD Editor, Hide Vanilla Effects (+ будущие).
  - **Категории (4):** Combat, Visuals, Player, Misc — заменяют процедурный `Icon` в rail.
  - **Служебные:** search (заменяет `Icon.SEARCH`), logo-mark CLUB.
- Судьба `Icon` (процедурного): после миграции rail/поиска остаётся только как чертёжный
  инструмент, если где-то ещё нужен; вероятно удаляется в конце 11.5 (решим по факту).

## 4. Логотип CLUB

Сейчас: акцентный квадрат 8×8 + слово «CLUB» (шапка меню, watermark редактора). Делаем настоящий
знак (logo-mark): в 11.3 готовлю **доску вариантов** (как с палитрой) → владелец выбирает →
знак попадает в тот же иконочный атлас и заменяет квадрат в обеих точках.

## 5. Конвейер (11.2) — реализовано, с уточнением инструмента

**Амендмент (реализация):** вместо скачивания msdfgen написан **собственный точный SDF-генератор**
`buildSrc/src/main/java/club/tools/IconAtlasGen.java` — для наших round-cap-штрихов дистанция
считается аналитически (`dist(точка, центр-линия) − width/2`), что а) точнее msdfgen-по-обведённому-SVG,
б) убирает шаг «обводка → заливка» и сетевую загрузку целиком, в) тестируется юнитами. Шейдеру
всё равно: grayscale-SDF в RGB, `median(r,r,r)==r`. Острые углы (фишка «M» в MSDF) нашему
круглому стилю не нужны.

```
tools/icons/src/<HHHH>_<name>.svg   (HHHH = PUA-кодпоинт глифа)
   └─ ./gradlew genIconAtlas  → src/main/resources/assets/club/ui/icon/msdf/icons.{png,json}
   └─ ./gradlew genIconPreviews → build/icon-previews/board.png (софт-симуляция шейдера:
        17/24/40/72px + ghost 9% — проверка качества без запуска клиента)
```

SVG-подмножество генератора: `path` (M m L l H h V v C c Q q Z z), `circle`, `line`, `rect`;
`stroke-width` (по умолч. 2), круглые концы всегда; `fill="solid"` на circle/rect = заливка.
Stage 15: `fill="solid"` на `path` = цельное тело по правилу even-odd — внутренний subpath
пробивает ДЫРУ (негативные детали массивных силуэтов: прорезь пояса, манжеты ботов).
Обводить штрихи в заливки НЕ нужно (в отличие от msdfgen-плана). Всё вне подмножества — ошибка
генерации (fail loud).

Рантайм: атлас = 4-й `MsdfAtlas` (`FontRegistry` резолвит PUA U+E000..F8FF через icon-seam;
падение загрузки атласа деградирует в '?', не ломая текст), рисуется **существующим** шейдером
`ui_msdf_text` (иконка = глиф). `IconGlyph.draw(ctx,x,y,size,color)` прибивает контент-бокс к
(x,y), компенсируя ascent шрифта. LEGACY: иконки тихо пропускаются. Ghost = тот же глиф крупно
с малой альфой под клипом карточки. Синхронизацию enum ↔ атлас держит `IconAtlasTest`.

**Приёмка 11.2: пройдена** — превью-доска подтвердила резкость 17→72px, включая диагонали.

## 6. Этапы и контрольные точки

| Этап | Содержание | Коммит | Точка владельца |
|---|---|---|---|
| 11.1 | Эта спека + токены `CategoryAccents` (palette A) | `5dfa797` | — |
| 11.2 | Конвейер: собственный SDF-генератор (buildSrc) + `genIconAtlas`/`genIconPreviews` + icon-seam + `IconGlyph` + тесты | `9aa2620` | техника ок ✔ (превью) |
| 11.3 | 12 иконок + выбор лого (**трефа** — карточная масть ♣: «Club» и есть трефа) | `4081b0c` | стиль — по доске превью |
| 11.4 | Карточка (чип/центр-полоса/ghost/состояния) + rail в категорийных цветах (eased) + лого в шапке и watermark + 14-й глиф | `7ba4f25` | **ин-гейм проверка владельцем — ожидается** |
| 11.5 | Удаление процедурного `Icon`, полировка, ревью | | финал стадии |

**Лого (утверждено реализацией):** цельная трефа ♣ (E021) — «Club» = трефовая масть; отклонены
бейдж-вариант и «C+точка» (доска вариантов в истории коммита 4081b0c).

**Расширение Stage 14 (HUD-пак):** тот же конвейер, +31 глиф — броня `0xE030–0xE034`
(шлем/нагрудник/поножи/бот/элитра; в HUD тонируются по материалу) и эффекты `0xE040–0xE059`
(по иконке на каждый «живой» ванильный эффект: шевроны-скорость, гантеля-сила, призрак-невидимость,
череп-иссушение, капля-яд…; `E059` — пробирка-fallback для редких/модовых). Ассоциативные цвета
эффектов живут в `com.club.ui.hud.EffectStyles` (не в токенах — это данные HUD, не тема).
Атлас: 45 глифов, 616×616. См. `HUD-LANGUAGE.md` (амендмент Stage 14).
**Stage 17:** HUD рисует дуотон ванильных текстур (`com.club.hud.PixelIcons`); эти глифы —
только фоллбек (LEGACY / битая текстура), HUD-пак больше не расширяется.

## 7. Риски / открытое

- **R6 (rect-only rounded clip):** ghost у скруглённого угла карточки может резаться квадратно —
  на альфе 5–8% ожидаемо незаметно; проверяем глазами в 11.4, лечим только если видно.
- **msdfgen и multi-path SVG:** страховка — правило «один составной path на иконку» (см. §3).
- **Off-канал серых** — выбрать из существующих токенов в 11.4.
- ~~Категорийный цвет в **поповере** сознательно НЕ используется~~ — **отменено 11.9** (см. §1):
  категорийный цвет = интерактив внутри поповера (слайдеры/тумблеры/фокус).
